<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.3/css/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="{{ path("casita") }}">freeCodeCamp</a>
            <!--el casita es el name de la pagina index-->
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("customPage", {name: "beau carnes"}) }}">Greet beau carnes</a>
                        <!--uso "name" porque asi esta en la url @Route("/custom/{name?}", name="customPage")-->
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("post.index") }}">Posts</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("post.create") }}">Create</a>
                    </li>
                    {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                        <li class="nav-item float-roght">
                            <a class="nav-link" href="{{ path("app_logout") }}">Logout</a>
                        </li>
                    {% endif %}
                </ul>
            <!--</div>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("post.index") }}">Posts</a>
                    </li>
                </ul>
            </div>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("post.create") }}">Create</a>
                    </li>
                </ul>
            </div>-->

        </nav>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
